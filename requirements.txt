# Core scientific stack
numpy>=1.21
pandas>=1.3
scipy>=1.7
scikit-learn>=1.0

# Spatial transcriptomics / single-cell
scanpy>=1.9
anndata>=0.8

# Deep learning
torch>=1.13
tensorflow>=2.10          # runs in TF1 compatibility mode (tf.compat.v1)

# GPU-accelerated array — choose the package that matches your CUDA version:
#   CUDA 11.x  →  cupy-cuda11x
#   CUDA 12.x  →  cupy-cuda12x
# Check your version with: nvcc --version
cupy-cuda11x

# Graph / network
networkx>=2.6
torch-geometric            # optional: used in GCNtest.py

# Cell-cell communication benchmarks
commot>=0.0.4              # optional: eval_other_methods.py
POT                        # optional transport (ot)

# Circular / chord plots
pycirclize>=0.5            # optional: figure scripts

# Visualisation
matplotlib>=3.5

# Utilities
tqdm>=4.60
psutil>=5.9
